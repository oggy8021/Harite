[pytest]
minversion = 6.0
addopts = -q -ra
testpaths = tests
python_files = test_*.py
markers =
    slow: marks tests as slow (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
