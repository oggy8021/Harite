## Harite 作業予定（日本語）

目的
- このファイルは私（アシスタント）とあなた（リポジトリ管理者）が合意した作業項目を日本語でまとめ、`discuss/todo-planning` ブランチ上で議論・調整するための起点です。

合意手順（短く）
1. 私がこのファイルを `discuss/todo-planning` に配置しました。
2. あなたが内容を確認し、必要な変更をコメントしてください。
3. ルール（ブランチ命名 / PR フロー / CI 待ち）に従って作業を開始します。自動で main に直接コミットはしません。
4. 合意できたら、私に作業を開始するよう指示してください。私が各タスクを小さな PR に分けて実行します。

重要な運用ルール（要約）
- PR は `feature/|fix/|docs/|chore/` のプレフィックスで作ること（既存の `discuss/todo-planning` は議論用に作成済）。
- 直接 `main` へは push せず、PR を介して CI が通るのを待ってから squash マージすること。
- `save/*` ブランチはバックアップ扱いで PR チェックをスキップする設定がある。

合意済みの作業項目（優先度順）
1. ドキュメント整理（Docs consolidation plan） — `docs/` の重複・表記ゆれの統合。小さな PR に分割。
2. `docs` 統合用 PR の作成（レビュー用）。
3. バックアップとブランチ運用ポリシーのドキュメント化。
4. `Improve XFCE heuristics` の調査開始（実装は別ブランチ `feature/`）。
5. CI: sdist/wheel ビルド job の追加。
6. リリース準備チェックリスト作成。
7. ブランチ保護・PR フローのペアセッション（スケジュール）。
8. 定期的なブランチクリーンアップ（運用ルール化）。

短い作業フロー提案
1. `discuss/todo-planning` 上で Docs consolidation の小タスクを洗い出す（このファイルを起点に）。
2. 各タスクを `feature/docs-consolidate-xxx` のような feature ブランチで実装し、PR を作る。
3. CI 通過 → あなたが squash マージ → 私がマージ後の確認を実施。

承認リクエスト
- この `docs/TODOs_jp.md` をスタート地点として合意しますか？
- 合意後、私が最初の小さなタスク（typo 修正など）を `feature/docs-consolidate-001` で始めてよいですか？

（補足）このファイルは `discuss/todo-planning` ブランチに置かれます。合意ができ次第、タスクを分解して私が順に実行します。

---
作成日: 2026-03-14
