# closed issues

GitHub で close 済みの Issue 観測ログ。resolution 節と正本への反映先を残す。

- 新規観測は親ディレクトリ `docs/online-issues/issue-{番号}.md` に置く。
- 解決後は `git mv` で本ディレクトリへ移動する。
- 索引は [../README.md](../README.md) を更新する。
