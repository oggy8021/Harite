# 開発進行における Copilot とオーナー（oggy8021）のルール

この文書は、プロジェクトにおける私（Copilot）とオーナーの間で合意した運用ルールをまとめます。小さな変更は随時更新し、重要な変更がある場合は履歴を残してください。

## 目的
- 作業フローを明確にし、無用な実装や承認漏れを防ぐ。

## 基本ルール
- 仕様書先行：機能追加や設計変更は、原則として仕様書（`docs/specs/*.md`）を作成・承認してから実装する。
- ドキュメント言語：仕様書・README・コメント・コミットメッセージは日本語を基本とする。
- コミット先：小規模なドキュメント更新や軽微な修正は `main` へ直接コミットして良い。大きな設計変更や実装はブランチを切り、PRでレビューする。
  
	[要確認: 現在のドキュメント整備方針では、広範な `docs/` の一括整備を `feature/docs-consolidate-001` 上で行う運用としています。`main` への直接コミット許可がある箇所は運用ルールと齟齬がないか要確認。]
- 承認：実装はプロジェクトオーナーの承認が必要。オーナーが1人である場合も、承認レスポンスをファイルで残す（ファイル編集かチャットでの明示）。
- 相談事項：指針に明記されていない判断や重大な設計決定は必ずオーナーへ相談する。

## Copilot の役割と制約
- 私は仕様書の草案作成、解析、雛形生成、CI設定、テスト作成などを行う。実装の着手はオーナー承認後に行う。
- 誤解や不明点が発生した場合、必ず確認を求める。勝手な解釈で進めない。
- セキュリティや秘密情報（APIキー等）はリポジトリに書き込まない。

## コミュニケーションと記録
- 重要な決定は `docs/` 内の該当ファイルに記録し、コミットする。
- オーナーの返答は該当ファイルに「回答（オーナー名／YYYY.MM.DD）：...」で追記する。

## 催促・先回り質問の禁止
- Copilot（自動化エージェント）は、作業の進行を促すために将来の複数ステップ先の問いかけや催促を行ってはなりません。
- 具体的には、次のような問いかけを控えます:
	- 「次にこれをやりますか？」等の将来予定の度重なる催促
	- 未承認の前提で複数ステップ先の意思決定を要求する質問
- ただし、現在のタスクを完了するために必要な最低限の確認（不足情報の補足や明確化）は積極的に行ってよいものとします。


## ルール更新方法
- ルールの変更は提案→オーナー承認で反映する。小さな補足は直接追記して構わないが、意味のある変更は承認を得る。

---

必要に応じてこのファイルを拡張します。

## ブランチ運用ルール（必須）

- 新しい作業（実装・設計・ドキュメントを含む）に着手する前に、必ず `main` ブランチへ戻り、最新を取得してください (`git checkout main` + `git pull`)。
- その後、新しいブランチを作成してください。ブランチ名は次の形式を原則とします：
	- 機能追加: `feature/<短い説明>`
	- 修正: `fix/<短い説明>`
	- ドキュメント: `docs/<短い説明>`
- 既存のブランチを流用して「別テーマ」を追加しないでください。別テーマや別用途の資材をそのまま同一ブランチに追加することを禁止します。
- 前ブランチが未マージで残っている場合は、以下のいずれかを行ってから新しい作業を始めてください：
	1.そのブランチを `main` にマージしてクローズする（PRを通じてレビュー・マージ）。
	2.ブランチを明確にアーカイブ（例: `archive/<name>` にリネーム）し、再利用しない旨をコミットメッセージで記録する。
	3.本当に継続が必要な場合は、そのブランチでの作業範囲を限定し、必ずオーナーの明示的承認を得る。

## パッケージング（Deb/.deb）決定プロセス（合意手順）

パッケージング方針は技術的影響が大きいため、以下の順序で合意を得ます。

1.Issue を作成して目的と選択肢を整理する（例: `fpm` 試作、`dh-python` ネイティブ、`dh-virtualenv` バンドル）。
2.各オプションのメリット・デメリットと前提（必要な Python バージョン、配布サイズ、メンテナンス負荷など）を `docs/` に短い比較表で記載する。
3.プロトタイプ案を1つ選び、小さな PoC ブランチで実装して実機（または VM）で動作確認をする。PoC は `feature/packaging-poc-<method>` のようなブランチ名で行う。
4.PoC の結果を Issue に報告し、オーナーと関係者の合意を得る。
5.合意された方式に基づき、正式実装を別ブランチで行い、PR を通じてレビューして `main` にマージする。

各段階で合意がない場合は、作業を中断し、必ずオーナーへ相談してください。

---

このセクションは直ちに適用されます。従来の振る舞いを変更するため、既存の未整理ブランチについてはオーナーの指示に従ってください。

## 開発ワークフローの追加ルール（安全性向上）

以下は「追跡の直進性」「ブランチの重ね掛け禁止」「トピック分離」を運用で守るための具体的なルールです。明確で実行しやすい手順を優先します。

- 追跡の直進性（履歴の分かりやすさ）:
	- `main` は常に安定な状態を保つ。`main` にコミットする前は必ずローカルでテスト／lint を通す。
	- マージは原則として PR 経由で行い、PR はマージ前に CI（lint + tests）が成功していることを必須条件とする。
	- 履歴は読みやすく保つため、機能ブランチは定期的に `main` をリベース（または最新をマージ）してコンフリクトを解消する。マージ方式（merge commit を残す/スカッシュしてマージする）はチーム合意に従う。

- `branch に branch を重ねない`（ブランチの派生ルール）:
	- 新しいトピックは必ず `main` からブランチを切る。既存ブランチから直接派生して作業を続けない。
	- どうしても既存ブランチの成果を使う必要がある場合は、そのブランチを `main` にまずマージ（または `main` に取り込み）してから新ブランチを作成する。

- ブランチは単一トピック（単一目的）:
	- 1 ブランチ = 1 トピック（機能／バグ／ドキュメント）。複数テーマを含むコミットは禁止。
	- 小さな PR を心がけ、変更点はレビューしやすい単位に分割する。

- その他の安全措置（推奨）:
	- `main` ブランチはリモートで保護（Protected Branch）設定を推奨：直接 push を禁止、必須 PR レビュー、CI 成功をマージ条件にする。
	- PR テンプレートを使って目的・範囲・動作確認手順を明示する（`.github/PULL_REQUEST_TEMPLATE.md` を活用）。
	- `CODEOWNERS` を設定して、重要領域の責任者によるレビューを必須化する（オーナー承認ポリシーと整合）。
	- 重大な変更（パッケージング、リリース方針、セキュリティ関連）は Issue で合意を得てから実装する。
	- 自動化: 可能な範囲で PR チェック（ブランチ名パターン、PR に Issue 番号を含める等）の GitHub Actions を設ける。

これらのルールは運用開始後に必要に応じて微調整します。大きく方針を変える場合は Issue を立て、オーナー承認を得てから適用してください。