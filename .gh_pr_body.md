
feat(plugins): プラグイン化による壁紙適用機能の追加 (feature/wallpaper-plugin)

概要
----

この PR は壁紙適用をプラグイン化するための最小限の基盤を追加します。
プラグインとして Windows、macOS、Linux のスタブを用意し、CLI に
`apply` コマンドを追加しました。デフォルトは dry-run（実際には変更しない）で、
実際にシステム設定を変更するには `--do-it` を指定します。

変更点
----

- `src/harite/plugins.py`: プラグインレジストリと `windows` / `macos` / `linux` プラグインを追加。
  - `linux` プラグインは GNOME (`gsettings`)、XFCE (`xfconf-query`)、`feh` を順に試すベストエフォート方式です。
- `src/harite/cli.py`: `apply` コマンドを追加（オプション: `--plugin`, `--file`, `--do-it`）。
- `tests/test_plugins.py`: レジストリと各プラグインの dry-run / missing-file のユニットテストを追加。
- `README.md`: `--do-it` の安全上の注意を追記。

動作確認
----

- ローカルでユニットテストを実行し、すべて成功しました（`pytest`：15 passed）。
- 各プラグインは dry-run 安全に実装されています。`--do-it` はプラットフォーム依存の操作です。

注意点 / 今後の作業
----

- XFCE のプロパティ名や設定は環境によって異なるため、`xfconf-query` の検出はベストエフォート実装です。
  WorkSpace 自動検出が main に合流した後で、より確実なプロパティマッピングを追加する予定です。
- PR の本文やタイトルの表現、追加テスト、CI ログに基づく修正などが必要であれば指示してください。

以上、レビューをお願いします。
